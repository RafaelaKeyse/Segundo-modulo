package com.exemplo;

// Interface que define a estratégia de publicação
public interface PublicavelInterface {
    // O método publicar agora recebe um conteúdo String
    void publicar(String conteudo);
}
